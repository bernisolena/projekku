<!doctype html>
<html lang="id">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Selamat Datang</title>
  <style>
    :root{
      --bg1: #6a11cb;
      --bg2: #2575fc;
      --card: rgba(255,255,255,0.12);
      --glass: rgba(255,255,255,0.08);
      --text: #ffffff;
    }

    *{box-sizing:border-box;margin:0;padding:0}
    html,body{height:100%}

    body{
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial;
      background: linear-gradient(135deg,var(--bg1),var(--bg2));
      color:var(--text);
      display:flex;
      align-items:center;
      justify-content:center;
      padding:2rem;
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
    }

    .container{
      width:100%;
      max-width:900px;
      background: linear-gradient(180deg, rgba(255,255,255,0.04), rgba(255,255,255,0.02));
      border-radius:14px;
      box-shadow: 0 8px 30px rgba(0,0,0,0.35);
      padding: clamp(1.25rem, 3vw, 2.5rem);
      backdrop-filter: blur(6px);
      border: 1px solid rgba(255,255,255,0.06);
      text-align:center;
      animation: floatIn 700ms ease-out both;
    }

    h1{
      font-size: clamp(1.8rem, 4.5vw, 3.2rem);
      line-height:1.05;
      margin-bottom:0.5rem;
      letter-spacing: -0.02em;
    }

    p.lead{
      font-size: clamp(1rem, 2.2vw, 1.125rem);
      opacity:0.95;
      margin-bottom:1.4rem;
    }

    .cta{
      display:inline-block;
      padding: .65rem 1.1rem;
      background: linear-gradient(90deg, rgba(255,255,255,0.14), rgba(255,255,255,0.08));
      color:var(--text);
      border-radius:10px;
      text-decoration:none;
      font-weight:600;
      border: 1px solid rgba(255,255,255,0.08);
      transition: transform .15s ease, box-shadow .15s ease;
      box-shadow: 0 6px 18px rgba(0,0,0,0.25);
    }

    .cta:hover{ transform: translateY(-3px); box-shadow: 0 12px 30px rgba(0,0,0,0.35); }

    footer{
      margin-top:1.1rem;
      font-size:.85rem;
      opacity:0.85;
    }

    @keyframes floatIn{
      from{ transform: translateY(12px); opacity:0 }
      to{ transform: translateY(0); opacity:1 }
    }

    /* Small decorative accent */
    .accent{
      width:72px;height:72px;border-radius:14px;margin:0 auto 1rem;
      background: linear-gradient(135deg, rgba(255,255,255,0.18), rgba(255,255,255,0.06));
      display:flex;align-items:center;justify-content:center;
      font-weight:700;color:rgba(0,0,0,0.12);font-size:1.15rem;
      box-shadow: inset 0 -8px 18px rgba(255,255,255,0.04);
    }

    @media (prefers-reduced-motion: reduce){
      .container{ animation: none }
      .cta{ transition: none }
    }
  </style>
</head>
<body>
  <main class="container" role="main" aria-labelledby="welcome-heading">
    <div class="accent" aria-hidden="true">★</div>
    <h1 id="welcome-heading">Selamat Datang</h1>
    <p class="lead">Terima kasih telah mengunjungi — semoga harimu menyenangkan!</p>
    <a class="cta" href="#" onclick="return false">Mulai</a>
    <footer>Halaman statis sederhana — simpan file ini sebagai <strong>index.html</strong> dan buka di browser.</footer>
  </main>
</body>
</html>
